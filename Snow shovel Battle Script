local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
    Name = "Snow Shovel Battle Script",
    Icon = 0, -- Icon in Topbar. Can use Lucide Icons (string) or Roblox Image (number). 0 to use no icon (default).
    LoadingTitle = "Loading",
    LoadingSubtitle = "by It's Kyan",
    ShowText = "Rayfield", -- for mobile users to unhide Rayfield, change if you'd like
    Theme = "Default", -- Check https://docs.sirius.menu/rayfield/configuration/themes

    ToggleUIKeybind = "K", -- The keybind to toggle the UI visibility

    DisableRayfieldPrompts = false,
    DisableBuildWarnings = false, -- Prevents Rayfield from emitting warnings when the script has a version mismatch

    ConfigurationSaving = {
        Enabled = true,
        FolderName = nil, -- Create a custom folder for your hub/game
        FileName = "Big Hub"
    },

    Discord = {
        Enabled = false, -- Prompt the user to join your Discord server if their executor supports it
        Invite = "noinvitelink", -- The Discord invite code, do not include Discord.gg/
        RememberJoins = true
    },

    KeySystem = true, 
    KeySettings = {
        Title = "Key System",
        Subtitle = "Key System",
        Note = "Key : Easy", -- Use this to tell the user how to get a key
        FileName = "Key", -- It is recommended to use something unique
        SaveKey = true,
        GrabKeyFromSite = false,
        Key = {"Easy"} -- List of keys that the system will accept
    }
})

-- Create Tab
local Tab = Window:CreateTab("Main", 4483362458) -- Title, Image (you can change the image ID)

-- Buttons
local Button = Tab:CreateButton({
    Name = "✨ INF TRAIN ✨",
    Callback = function()
        local args = {
            math.huge
        }
        game:GetService("ReplicatedStorage"):WaitForChild("Event"):WaitForChild("Train"):FireServer(unpack(args))
    end,
})

local Button = Tab:CreateButton({
    Name = "🔥 INF WINGAIN 🔥",
    Callback = function()
        local args = {
            math.huge
        }
        game:GetService("ReplicatedStorage"):WaitForChild("Event"):WaitForChild("WinGain"):FireServer(unpack(args))
    end,
})

local Button = Tab:CreateButton({
    Name = "Rebirth Adder",
    Callback = function()
        local args = {25}
        game:GetService("ReplicatedStorage"):WaitForChild("Event"):WaitForChild("HealthAdd"):FireServer(unpack(args))
    end,
})

local Button = Tab:CreateButton({
    Name = "Upgrade Adder",
    Callback = function()
        local args = {
            2250000000000000,
            0.15
        }
        game:GetService("ReplicatedStorage"):WaitForChild("Event"):WaitForChild("Enchanted"):FireServer(unpack(args))
    end,
})

local Button = Tab:CreateButton({
    Name = "Pet Giver",
    Callback = function()
        local args = {
            "Abyss",
            "Soul Warden",
            6654456
        }
        game:GetService("ReplicatedStorage"):WaitForChild("PEV"):WaitForChild("Hatch"):FireServer(unpack(args))
    end,
})

local Button = Tab:CreateButton({
    Name = "Equip Best Shovel",
    Callback = function()
        local args = {
            "Starry",
            game:GetService("Players").LocalPlayer.Character
        }
        game:GetService("ReplicatedStorage"):WaitForChild("Event"):WaitForChild("EquipEffect"):FireServer(unpack(args))
    end,
})
