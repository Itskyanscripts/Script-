--This is My Own Script 
local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()
local Window = Rayfield:CreateWindow({
   Name = "Snow shovel battle script",
   Icon = 0,
   LoadingTitle = "script is loading",
   LoadingSubtitle = "by Kyan",
   ShowText = "Rayfield",
   Theme = "Default",

   ToggleUIKeybind = "K",

   DisableRayfieldPrompts = false,
   DisableBuildWarnings = false,

   ConfigurationSaving = {
      Enabled = true,
      FolderName = nil,
      FileName = "Big Hub"
   },

   Discord = {
      Enabled = false,
      Invite = "noinvitelink",
      RememberJoins = true
   },

   KeySystem = true,
   KeySettings = {
      Title = "Key System",
      Subtitle = "Key System",
      Note = "Visit this link to get the key → https://link-hub.net/3727416/eDBd1DA4ogAT",
      FileName = "Key",
      SaveKey = false,         
      GrabKeyFromSite = true,
      Key = {"Troll", "Simple"}
   }
})

local Tab = Window:CreateTab("Tab Example", 4483362458)

local Input = Tab:CreateInput({
   Name = "Strength Changer",
   CurrentValue = "",
   PlaceholderText = "Enter amount",
   RemoveTextAfterFocusLost = false,
   Flag = "Input1",
   Callback = function(Text)
      local args = {tonumber(Text)}
      game:GetService("ReplicatedStorage"):WaitForChild("Event"):WaitForChild("Train"):FireServer(unpack(args))
   end,
})

local Input = Tab:CreateInput({
   Name = "WinGain Changer",
   CurrentValue = "",
   PlaceholderText = "Input Placeholder",
   RemoveTextAfterFocusLost = false,
   Flag = "Input2",
   Callback = function(Text)
      local args = {tonumber(Text)}
      game:GetService("ReplicatedStorage"):WaitForChild("Event"):WaitForChild("WinGain"):FireServer(unpack(args))
   end,
})

getgenv().AutoRebirth = false

local Toggle = Tab:CreateToggle({
   Name = "Auto Rebirth",
   CurrentValue = false,
   Flag = "Toggle1",
   Callback = function(Value)
      getgenv().AutoRebirth = Value
      if Value then
         spawn(function()
            while getgenv().AutoRebirth and task.wait(0.3) do
               local args = {25}
               pcall(function()
                  game:GetService("ReplicatedStorage"):WaitForChild("Event"):WaitForChild("HealthAdd"):FireServer(unpack(args))
               end)
            end
         end)
      end
   end,
})

local Button = Tab:CreateButton({
    Name = "Upgrade Adder",
    Callback = function()
        local args = {2250000000000000, 0.15}
        game:GetService("ReplicatedStorage"):WaitForChild("Event"):WaitForChild("Enchanted"):FireServer(unpack(args))
    end,
})

local Button = Tab:CreateButton({
    Name = "Pet Giver",
    Callback = function()
        local args = {"Abyss", "Soul Warden", 6654456}
        game:GetService("ReplicatedStorage"):WaitForChild("PEV"):WaitForChild("Hatch"):FireServer(unpack(args))
    end,
})

local Button = Tab:CreateButton({
    Name = "Equip Best Shovel",
    Callback = function()
        local args = {"Starry", game:GetService("Players").LocalPlayer.Character}
        game:GetService("ReplicatedStorage"):WaitForChild("Event"):WaitForChild("EquipEffect"):FireServer(unpack(args))
    end,
})
